#Intializing Master Node

kubeadm init --pod-network-cidr=10.10.1.0/16



#Run flannel command so that Slave can connect to master

kubectl apply -f https://raw.githubusercontent.com/coreos/flannel/master/Documentation/kube-flannel.yml




Kubectl get nodes                          ---> will show you nodes inside cluster

kubectl get ns                             ---> will show you namespace       

kubectl get pods -o wide                   ---> will show you pods in each node

kubectl get pods --all-namesapce           ----> pods running in all namespace

